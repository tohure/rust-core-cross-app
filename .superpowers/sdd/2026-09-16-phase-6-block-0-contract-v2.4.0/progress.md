# SDD ledger — plan: docs/superpowers/plans/2026-09-16-phase-6-block-0-contract-v2.4.0.md

Spec: docs/superpowers/specs/2026-09-16-phase-6-hardening-design.md (leída, alcanzable)
Rama: feat/phase-6-hardening (no es main; se trabaja en el checkout principal)

Ruling: no se crea worktree aislado — se trabaja en la rama `feat/phase-6-hardening` del
checkout principal. Motivo: las tareas 8-11 construyen artefactos nativos (cargo-ndk, Xcode,
ubrn) cuyas rutas y cachés están atadas a este checkout, y un worktree obligaría a reconstruir
toda la toolchain. Costo si me equivoco: el árbol de trabajo del usuario queda ocupado por esta
fase mientras dure; se revierte con `git checkout main`.

## Escaneo previo de conflictos

| # | Par / tarea | Produce → consume | Hallazgo |
|---|---|---|---|
| 1 | T1 → T2, T3 | `DomainError::Decryption` / `contract_name() = "Descifrado"` | OK. T2 y T3 declaran el consumo y usan el mismo nombre exacto. |
| 2 | T1 → T4 | el string `"Descifrado"` | OK. T4 lo usa como valor del campo `error` y como clave de `messages.es.json`. |
| 3 | T2 (consigo misma) | tests nuevos vs. tests existentes de `crypto.rs` | OK. El Step 4 nombra los cuatro asserts que cambian de `"Cifrado"` a `"Descifrado"` y dice explícitamente que no se borra ningún test. |
| 4 | T3 → T8-T11 | enum uniffi regenerado | OK. Las cuatro tareas de app regeneran antes de compilar. |
| 5 | **T4 → T5** | `cases.json` v2.4.0 / guardias | **CONFLICTO REAL**: el commit de T4 deja el test de contrato de Rust en rojo hasta que T5 lo cierra. Ver ruling abajo. |
| 6 | T4 → T6 | textos de `messages.es.json` / tabla de `rust-core/README.md` | OK. T6 verifica la igualdad literal con un script. |
| 7 | T5 → T8-T11 | `contract_decrypt` como referencia a espejar | OK. Los valores exactos (2.4.0, 3 casos, contador en 3) están repetidos en cada tarea, no por referencia. |
| 8 | T7 → T8-T12 | la secuencia de generación documentada | OK. T7 va antes que todas las que regeneran. |
| 9 | T10 → T11 | el paquete WASM v2.4.0 | OK. El plan lo dice explícitamente y el orden lo respeta. |
| 10 | **T8-T11 → T12** | artefactos regenerados | **Duplicación intencional**: T12 vuelve a regenerar los cuatro. Ver ruling abajo. |
| 11 | T4 (consigo misma) | conteos del Step 3 | OK. 12 claves + `descifrado` = 13; 9 mensajes + `Descifrado` = 10; 28 + 3 = 31. Coincide con Global Constraints. |
| 12 | T5 (consigo misma) | tabla `EXPECTED` de 6 grupos | OK. `cuentas_iniciales` sigue fuera del conteo, como hoy: es fixture, no grupo de casos. |
| 13 | T8, T9, T10, T11 (cada una) | guardia de versión vs. recorrido nuevo | OK. Cada una sube la versión Y agrega el recorrido; ninguna hace una sola de las dos. |

Ruling (fila 5): el commit de `contracts/*.json` se deja solo aunque deje el test de contrato en
rojo por un commit. Motivo: el CLAUDE.md lo exige — «Corregir un valor esperado en cases.json
siempre va en su propio commit» — y es la única forma de auditar después si el contrato se dobló
para que pasara el código. La ventana en rojo dura exactamente un commit y la cierra T5. Costo si
me equivoco: alguien que haga checkout de ese commit puntual encuentra la suite roja.

Ruling (fila 10): T12 regenera de nuevo los cuatro artefactos aunque T8-T11 ya los regeneraron.
Motivo: `coreVersion()` congela el SHA de HEAD en tiempo de compilación, y T8-T11 commitean por
separado, así que cada app quedaría con un SHA distinto. Sin T12 los cuatro pies no coinciden y
el primer paso del runbook de demo falla. Costo si me equivoco: tiempo de build repetido.

Riesgo ambiental (no es conflicto del plan): T8 necesita dispositivo o emulador Android y T9
necesita Xcode con simulador o aparato. Si no están disponibles, esas tareas se paran y se
reporta; no se declaran completas sin la corrida.

## Progreso
Task 1: complete (commits 5de8a27..7f964db, review clean)
Task 2: implementada (commits 7f964db..65632b7), en revisión
Task 2: minor (deferred): dos ramas de error de `decrypt` —hex de clave no válido y texto descifrado no UTF-8— no tienen test que aserte la variante; la corrección está garantizada estructuralmente porque `prepare` ya no construye variantes, y el proptest `decrypt_never_panics` las ejercita sin verificar el nombre.
Task 2: complete (commits 7f964db..65632b7, review clean)
Task 3: complete (commits 65632b7..5d0b710, review clean)
Task 4: minor (deferred): el mensaje del commit d52f21e dice que de-001 es el hex de tj-001 "leído al revés", queriendo decir la operación inversa (descifrar) y no el string invertido. Redacción ambigua en un mensaje ya commiteado; reescribir historia por eso no se justifica.
Task 4: complete (commits 5d0b710..d52f21e, review clean — los tres vectores verificados ejecutando decrypt contra el core real)
Task 5: complete (commits d52f21e..24034ee, review clean — 12 tests de contrato, 71 en el workspace; ninguna guardia aflojada)
Task 6: implementada (commit 9023d5c) con DONE_WITH_CONCERNS.
Task 6: Ruling: la tabla de mensajes NO vive en `rust-core/README.md` como afirmaba el plan, sino en `rust-core/FFI.md` desde el commit 5a07492, cuando el README se partió en cinco archivos. El plan lo afirmó porque lo leyó del campo `_fuente` de `contracts/messages.es.json`, que quedó desactualizado en ese refactor. Decisión: la tabla NO se mueve —partir el README fue una decisión de diseño documentada—, se actualiza donde está, y se corrige `_fuente` para que deje de mentir. El plan estaba mal, no el implementador. Costo si me equivoco: `_fuente` apunta a un archivo distinto del que alguien espera; se revierte con una línea.
Task 6: fix round 1/5 en curso — corregir `_fuente` en messages.es.json (alcance ampliado a ese campo, y solo a ese) y cerrar las menciones residuales a "nueve"/2.3.0/28 casos en rust-core/*.md
Task 6: fix round 1/5 (1 addressed, 0 open — `_fuente` corregido y menciones residuales cerradas; commit 04fa661)
Task 6: complete (commits 24034ee..04fa661, review clean — los diez mensajes verificados idénticos entre FFI.md y messages.es.json)
Task 7: Ruling: el plan escribía `ubrn build web` para el paso de wasm y ese comando NO existe en el repositorio; el vigente es `pnpm run wasm:generate`, que envuelve `ubrn build wasm2 --release --and-generate --config ubrn.wasm.yaml`. El `--config` no es opcional: sin él se pisan los bindings JSI (hay un script `ubrn:wasm` que se borró por eso, documentado en apps/react-native/CONTEXT.md). Decisión: vale el comando real, no el del plan. Costo si me equivoco: la secuencia documentada no genera el wasm y la Task 11 (Angular) se queda sin artefacto — se detecta al ejecutarla.
Task 7: implementada (commit 6949bbe). Revisión: spec ✅, calidad no aprobada — 1 Important (dice que los bindings Kotlin quedan en `core/`, directorio que no existe; la ruta real es app/src/main/java/uniffi/core_financiero/) + 1 Minor (cita falsa en el reporte de auditoría, sin impacto en lo publicado).
Task 7: Ruling: el `core/` es una inexactitud HEREDADA del diagrama de CLAUDE.md ("apps/android (jniLibs/*.so + core/)"), no un invento del implementador. Decisión: se corrige en los dos lugares en el mismo commit, ampliando el alcance a CLAUDE.md solo para esa línea. Costo si me equivoco: una línea de diagrama distinta de lo que alguien esperaba leer.
Task 7: fix round 1/5 en curso
Task 7: fix round 1/5 (1 addressed, 0 open — ruta de los bindings corregida en BUILD.md y en el diagrama de CLAUDE.md; commit a82798c)
Task 7: complete (commits 04fa661..a82798c, review clean)
Task 8: implementada (commit 3a7e431) — JVM 29/29, instrumentada 16/16 sobre Pixel 6 real (25251FDF60033N).
Task 8: Ruling: el brief listaba solo las tres aserciones de versión de Android, pero la app espeja MÁS guardias del test de contrato de Rust: el conteo por grupo, el set de claves de primer nivel, la versión de messages.es.json y el conteo de mensajes en AssetSourcesTest. El implementador las corrigió todas por su cuenta. Decisión: correcto, quedan. Costo si me equivoco: ninguno visible — sin esos cambios la suite instrumentada quedaba roja.
Task 8: Ruling: `theMessagesAssetCoversTheNineErrorVariants` quedó cubriendo nueve variantes de diez y con un nombre que ya no es cierto. Aunque no falla, NO se difiere: una guardia que afirma cubrir algo que no cubre es el mismo defecto que esta fase vino a corregir en los PENDING.md. Entra en ronda de arreglo. Costo si me equivoco: una ronda de más sobre un test que ya pasaba.
Task 8: fix round 1/5 en curso
Task 8: fix round 1/5 (1 addressed, 0 open — `theMessagesAssetCoversTheNineErrorVariants` pasó a `…TheTenErrorVariants`, construye `DomainException.Decryption` y exige diez nombres de contrato distintos; commit e91f4ae). Suites re-corridas: JVM 29/29 y instrumentada 16/16.
Task 8: Ruling: la corrida instrumentada del fix round fue sobre el AVD `Pixel_9_Pro` y no sobre el Pixel 6, que no estaba conectado. Motivo: el AVD es arm64 (`sdk_gphone64_arm64`), así que carga el mismo slice `arm64-v8a` que el teléfono; y lo que este fix round cambió son aserciones de test, no el borde FFI, que ya quedó verificado sobre aparato real en el commit 3a7e431. Costo si me equivoco: la evidencia del fix round no es de hardware; se repite con `./gradlew :app:connectedDebugAndroidTest` con el teléfono enchufado.
Task 8: Hallazgo fuera de alcance (va a la Task 11 del bloque 1, documentación de Android): la v2.4.0 dejó desactualizados `apps/android/CONTEXT.md:66` («sus nueve subclases», son diez), `:70` («nueve líneas» del `when` de `contractName`), `:142` («los nueve textos de usuario») y `apps/android/README.md:194` («los 28 casos», son 31). No se tocaron acá porque el alcance de la Task 8 es código y tests, no docs.
Task 8: Hallazgo de repo (corregido aparte): `.superpowers/sdd/.gitignore` estaba modificado en el árbol de trabajo a `*` a secas, sin el `!*/` `!*/progress.md`. Efecto: el ledger de esta fase quedaba invisible para git mientras los de las fases 1, 3, 4 y 5 sí están versionados. Restaurado a la versión de HEAD y este ledger agregado al índice.
Task 8: complete (commits 3a7e431..e91f4ae, review inline). Ruling: la revisión del fix round la hice inline y no con un subagente revisor, a diferencia de las tareas 1-7. Motivo: el diff son seis archivos de test/comentario, ya leídos enteros, y la evidencia dura son las dos suites re-corridas (29/29 y 16/16). Costo si me equivoco: un par de ojos menos sobre un cambio que no toca produccion salvo comentarios.

Task 9: implementada (commit e49d27e) — 48 tests en 12 suites, 0 fallos, sobre el simulador iPhone 17 (iOS 27). El grupo nuevo corre como «descifrado with 3 test cases passed».
Task 9: Ruling: el plan pide el simulador `iPhone 16` y en esta máquina no existe (el Xcode instalado trae iPhone 17 / 17 Pro / 17e / Air). Se corrió sobre `iPhone 17`. Costo si me equivoco: ninguno visible — el slice es el mismo `aarch64-apple-ios-sim`; lo que no prueba, igual que antes, es el slice de aparato.
Task 9: Ruling: el plan dice «cerrando con el contador en 3, igual que hacen los otros grupos de ese archivo», pero los otros grupos de `ContractTest.swift` NO llevan contador propio: son `@Test(arguments:)` parametrizados y el conteo vive en la guardia `eachGroupHasTheExpectedNumberOfCases`. Se siguió el idioma real del archivo —test parametrizado + `#expect(contract.decrypt.count == 3)` en esa guardia— en vez del `checked += 1` de Kotlin y Rust. Costo si me equivoco: el conteo está en otra función que en las otras dos plataformas; un grupo vaciado igual falla.
Task 9: Desviación respecto del brief, misma clase que la de la Task 8: el plan listaba sólo `ContractTest.swift:11,19` y `ContractFixtures.swift:41`, pero había más guardias con el conteo viejo hardcodeado y todas habrían quedado rojas o mintiendo: el conteo de mensajes de `ContractFixtures.swift` (9 → 10), las tres guardias de «nueve variantes» de `ContractTest.swift` —incluida la que ata el `messages.es.json` del bundle, la única que Rust no puede dar— y `ContractMessagesTest.swift`, que el plan no mencionaba y que es el espejo del test JVM que Android sí tenía en su brief.
Task 9: Hallazgo de entorno, causa NO aislada: la primera corrida de `xcodebuild test` falló con `Failed to install or launch the test runner … No such process` tras 626 s, con el emulador Android todavía levantado de la Task 8 y el simulador sin arrancar. La segunda corrida pasó, pero cambié dos cosas a la vez —maté el emulador y pre-arranqué el simulador con `xcrun simctl boot`—, así que no sé cuál de las dos lo arregló. Lo dejo escrito porque el runbook de demo levanta las cuatro apps a la vez y este es el modo de fallo que ahí aparecería.

Task 10: Ruling: el mapeo de nombres de React Native NO es propio de la app — vive en `packages/contract/src/tags.ts` y lo comparten tres consumidores: el test de contrato de RN, la app de RN y Angular. O sea que la Task 10 y la Task 11 tocan el mismo archivo, cosa que el plan no dice: lo trata como "el mapeo de React Native" en la T10 y "el mapeo de Angular" en la T11. Se cambia UNA vez, en la Task 10, y la Task 11 lo encuentra hecho. Costo si me equivoco: la T11 no tiene que tocar el mapeo y su brief va a pedirlo igual.
Task 10: Hallazgo, y es el que más importa de esta tarea: **la guardia de tipos no rompió cuando debía.** `apps/react-native/src/guard.ts` existe para que `tsc` falle si el core agrega una variante y `CONTRACT_NAMES` no la tiene — es el equivalente del `when` de Kotlin y del `switch` de Swift. Tras regenerar N-API y WASM, `pnpm run typecheck` pasó **en verde** con la tabla todavía en nueve. Motivo: `guard.ts` importa `DomainError` de `./bindings`, que reexporta `src/generated/` — el flavour **JSI**, que ninguno de esos dos comandos regenera. La red sólo se arma después de `pnpm run ubrn:android` (o `ubrn:ios`). El Step 2 del plan pide regenerar las tres salidas, pero no dice que el ORDEN importa: regenerar sólo las dos que los tests usan deja la guardia dormida y el rojo lo tiene que encontrar Jest.
Task 10: Desviación de conteos del plan: dice que la suite sube «de 109 a 111 tests». La base real era **120** (7 rojos) y quedó en **126**: seis casos nuevos, no dos, porque el grupo `descifrado` trae 3 casos y se recorre por los DOS caminos (N-API y WASM), más las guardias. El 109 de CLAUDE.md quedó viejo.
Task 10: Desviación de archivos: además de los dos tests de contrato que el plan lista, hubo que tocar `packages/contract/src/__tests__/contractName.test.ts`, que hardcodea `toHaveLength(9)` sobre las claves de `CONTRACT_NAMES`. Estaba rojo desde antes de esta tarea junto con `tags.test.ts`, que compara los valores de la tabla contra las claves de `messages.es.json` — o sea que el commit del contrato (d52f21e) dejó tres suites de RN en rojo, no sólo las dos del plan.
Task 10: implementada (commits 41727e3 + 7729115) — 126 passed / 126 total, 16 suites. La guardia de tipos verificada a mano: sacándole `Decryption` a la tabla, `tsc` falla con TS2322 en `guard.ts:27`; con la entrada, verde.
Task 10: Ruling: `pnpm run ubrn:ios` termina en `pod install` y eso reescribió dos archivos VERSIONADOS por estado de la máquina y no por el contrato — `Podfile.lock` (CocoaPods 1.15.2 → 1.16.2) y `project.pbxproj` (el hash del store de pnpm dentro de `REACT_NATIVE_PATH`, mismo react-native 0.87.0). Fueron a su propio commit (7729115) en vez de ensuciar el del contrato. Costo si me equivoco: un commit de más en el historial de la fase.

Task 11: implementada (commit 9c2ab91) — 102 passed en 15 archivos. El plan esperaba «de 99 a 100»: son 102, porque el grupo trae tres casos y no uno. El mapeo NO se tocó acá, ya venía hecho de la Task 10: es el mismo `packages/contract/src/tags.ts`, tal como anticipó el ruling de esa tarea.

Task 12: Step 3 cumplido — los SEIS binarios (Android `.so`, los dos slices de iOS, el turbo module de RN, el `.dylib` de N-API y el `.wasm`) coinciden en `1.0.0+<sha>`, regenerados en una sola pasada encadenada.
Task 12: Hallazgo, y el comando del plan lo tiene mal: el Step 3 propone `grep -o '1\.0\.0+[0-9a-f]\{7,\}'`, con el cuantificador ABIERTO. Rust empaqueta los literales de string contiguos y sin terminador, así que `strings` los devuelve pegados y el patrón se come los caracteres hex del literal siguiente: Android, los dos de iOS y el N-API devolvían `1.0.0+56acc05ca` y el WASM y el turbo module `1.0.0+56acc05`, siendo los SEIS del mismo commit — el `ca` era el principio de «called `Result::unwrap()`». Un patrón abierto declara rota la comprobación que la demo mira primero. El patrón va anclado al SHA de HEAD. Quedó documentado, con su salida real, en `rust-core/README.md` § «Comprobar los cuatro artefactos de una vez» (commit 21ee36f), que es donde alguien lo va a buscar dentro de seis meses; el README ya traía la forma anclada para el `.dylib` del host, pero sólo para ése.
Task 12: Step 5 — las cinco suites en verde contra los artefactos regenerados: Rust 71 (3 lib de ffi + 12 de contrato + 50 de domain + 6 proptests), Android 29 JVM y 16 instrumentados, iOS 48, React Native 126, Angular 102. Los totales del plan para Rust (71), Android (16) e iOS (48) dieron exactos; los de RN (decía 111) y Angular (decía 100) no, por el mismo motivo ya anotado en las tareas 10 y 11.
Task 12: Ruling: la corrida de iOS del Step 5 fue **sobre el iPhone del usuario (iOS 18.7.8)**, no sobre simulador. Motivo: el aparato apareció conectado a mitad de la fase y es lo único que prueba que el slice `aarch64-apple-ios` enlaza y resuelve sus símbolos — eso cierra el ruling de la Task 9, que había quedado con evidencia sólo de simulador. La corrida de Android fue sobre el AVD `Pixel_9_Pro` (arm64), no sobre el Pixel 6, que no estaba conectado.
Task 12: Ruling: el Step 1 del plan («árbol limpio antes de regenerar») obliga a un orden que el plan no dice: la regeneración tiene que ser lo ÚLTIMO, después del último commit, porque cada commit mueve HEAD y con él el SHA que `build.rs` congela. Regenerar y después commitear deja los seis artefactos un commit atrás de HEAD y el propio comando del README —anclado a `git rev-parse --short HEAD`— imprime seis líneas vacías. Costo si me equivoco: una regeneración de ~10 minutos de más.
Task 12: Step 4 CUMPLIDO — los cuatro pies leídos de la pantalla real, todos `1.0.0+959025f`: Android nativo y React Native por `adb shell uiautomator dump` (el nodo de texto del árbol de UI), iOS por `xcrun simctl io screenshot` sobre el simulador iPhone 17, Angular por Chrome headless con `--dump-dom` contra el servidor estático (el `<p class="core-version-footer">`).
Task 12: CORRECCIÓN de un ruling que escribí mal. Había anotado el Step 4 como «no automatizable desde acá, es del usuario». Es falso: los tres mecanismos de arriba leen el texto renderizado sin intervención humana. El error fue tomar la redacción del runbook —que describe una demo en vivo, mirada por una persona— como si fuera una limitación técnica, sin preguntarme si podía leer la pantalla. Lo que sí necesita a una persona es la demo; la verificación no.
Task 12: Hallazgo de cómo se descubrió: el usuario reportó `1.0.0+3bd5cfc` en «Android» y `1.0.0+3090c8c` en iOS, ninguno de los dos el esperado. Diagnóstico, verificado y no supuesto: (a) la app Android nativa NO estaba instalada — `connectedAndroidTest` de AGP desinstala las dos APK al terminar, así que correr la suite instrumentada no deja nada en el aparato; lo que el usuario abrió fue la app de React Native de la Fase 5, instalada el 2026-09-13 00:20 (`dumpsys package`), que el snapshot del AVD restauró; (b) el simulador de iOS tenía el build de las 06:41, cuando HEAD era 3090c8c: regeneré el XCFramework y corrí la suite de simulador ANTES de commitear los cambios Swift. Ninguno de los dos era un artefacto desalineado: eran instalaciones viejas.
Task 12: Nota para quien verifique el pie del binario de iOS: `strings` sobre el ejecutable enlazado de la app (`ios-rust-test.app/ios-rust-test`) NO encuentra `1.0.0+<sha>`, aunque la pantalla lo muestre. Hay que mirarlo en el `.a` del XCFramework, que es lo que hace el bloque del README. En Android sí se puede mirar dentro del APK (`unzip -p <apk> lib/arm64-v8a/libcore_financiero.so`).
